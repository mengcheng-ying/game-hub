name: 百度链接自动推送

on:
  schedule:
    - cron: '0 7 * * *'   # 每天北京时间 15:00（UTC 07:00）定时全量推送
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      mode:
        description: '推送模式：incremental=增量(默认) / full=全量 / onebyone=逐条推到配额上限'
        required: false
        default: 'incremental'
        type: choice
        options:
          - incremental
          - full
          - onebyone

jobs:
  baidu-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # 取完整历史，保证 git diff HEAD~1 可用
          fetch-depth: 2

      - name: 推送链接到百度
        env:
          BAIDU_TOKEN: ${{ secrets.BAIDU_PUSH_TOKEN }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            if [ "${{ inputs.mode }}" = "full" ]; then
              node .github/scripts/baidu-push.js --all
            elif [ "${{ inputs.mode }}" = "onebyone" ]; then
              node .github/scripts/baidu-push-onebyone.js
            else
              node .github/scripts/baidu-push.js
            fi
          elif [ "${{ github.event_name }}" = "schedule" ]; then
            # 每日 15:00 定时全量推送，把当前全部链接提交给百度
            node .github/scripts/baidu-push.js --all
          else
            node .github/scripts/baidu-push.js
          fi
